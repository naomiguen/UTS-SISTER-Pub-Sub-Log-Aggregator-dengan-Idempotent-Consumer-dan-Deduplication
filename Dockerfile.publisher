FROM python:3.11-slim

WORKDIR /app

RUN adduser --disabled-password --gecos '' appuser \
    && chown -R appuser:appuser /app

USER appuser

COPY publisher_sim.py ./

CMD ["python", "publisher_sim.py"]